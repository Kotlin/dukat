plugins {
    id("kotlin")
}

sourceSets {
    main.kotlin.srcDirs = ['src']
}

dependencies {
    implementation(project(":ast"))
    compile(project(":ast-model-marker"))
    implementation(project(":ast-common"))
}