plugins {
  id("kotlin")
}

sourceSets {
  main.kotlin.srcDirs = ['src']
}


dependencies {
  compile(project(":interop"))
  compile "org.graalvm.sdk:graal-sdk:${gradle.graalVersion}"
  compile "org.graalvm.js:js:${gradle.graalVersion}"
}